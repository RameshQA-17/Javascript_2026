let names=["virat","rohit","yuvi"]

console.log(names);

names.push("ramesh")  //add element at the end
names.push("shivu")
names.push("pavan")

console.log(names);
 
names.pop();       //remove element at the end

console.log(names); 

names.unshift("murgesh"); //add the element to the first place

console.log(names);

names.shift();  //removes the element from 1st place

console.log(names);
